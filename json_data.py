class JsonPlaceholderData:
    
    def __init__(self, headers, encoding, userId, id, title, body):
        self.headers = headers
        self.encoding = encoding
        self.userId = userId
        self.id = id
        self.title = title
        self.body = body
    